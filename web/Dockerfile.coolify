# Thin wrapper — pulls the immutable image built for this Git commit.
# Coolify must have "Include Source Commit in Build" enabled so it passes
# SOURCE_COMMIT; GitHub Actions publishes the matching commit tag before deploy.
ARG SOURCE_COMMIT
FROM ghcr.io/trooper15a/collectr:${SOURCE_COMMIT}
